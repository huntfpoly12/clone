import gql from "graphql-tag";
export default gql`
mutation createMyCompanyAutoGeneratedSeal( 
            $companyId : Int!, 
            $seal : String!
        ) {
            createMyCompanyAutoGeneratedSeal( 
            companyId : $companyId,
            seal : $seal 
            ) 
            
}
`